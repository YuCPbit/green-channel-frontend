# 高校绿色通道系统前端

基于 Vue 3 和 Vite 的高校绿色通道系统前端应用。

## 本地启动

```bash
npm install
npm run dev
```

默认访问 `http://localhost:5173`，开发服务器会将 `/api` 代理到 `http://localhost:8080`。

生产构建：

```bash
npm run build
```

